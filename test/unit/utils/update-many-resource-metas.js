import {updateManyResourceMetas} from '../../../src';

describe('updateManyResourceMetas', function() {
  it('should be a function', () => {
    expect(updateManyResourceMetas).to.be.a('function');
  });
});
