{
  "name": "resourceful-redux-project",
  "private": true,
  "scripts": {
    "test": "npm run lint && cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text mocha --compilers js:babel-register --colors packages/resourceful-redux/test/setup/node.js packages/resourceful-redux/test/unit/**",
    "lint": "lerna run lint",
    "clean": "rimraf packages/*/dist tmp",
    "build": "npm run clean && lerna run build && cross-env NODE_ENV=production lerna run build",
    "prepublish": "in-publish && npm run build || not-in-publish",
    "bootstrap": "lerna bootstrap",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "docs:prepare": "gitbook install",
    "docs:clean": "rimraf _book",
    "docs:build": "npm run docs:prepare && gitbook build",
    "docs:publish": "npm run docs:clean && npm run docs:build && cp CNAME _book && cd _book && git init && git commit --allow-empty -m 'update book' && git checkout -b gh-pages && touch .nojekyll && git add . && git commit -am 'update book' && git push git@github.com:jmeas/resourceful-redux gh-pages --force"
  },
  "license": "MIT",
  "nyc": {
    "sourceMap": false,
    "instrument": false
  },
  "devDependencies": {
    "babel-core": "6.14.0",
    "babel-loader": "6.2.5",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-plugin-istanbul": "^4.1.4",
    "babel-polyfill": "6.13.0",
    "babel-preset-latest": "6.14.0",
    "babel-preset-stage-3": "^6.17.0",
    "babel-register": "6.14.0",
    "chai": "3.5.0",
    "cross-env": "^5.0.1",
    "gitbook-cli": "^2.3.0",
    "gitbook-plugin-ga": "^2.0.0",
    "in-publish": "^2.0.0",
    "lerna": "^2.0.0-rc.5",
    "mocha": "^3.0.2",
    "nyc": "^11.0.3",
    "sinon": "1.17.5",
    "sinon-chai": "2.8.0"
  }
}
