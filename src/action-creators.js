import xhr from 'xhr';

export default function(actionTypes) {
  return {
    create() {
      // return (dispatch, getState) => {
      //   dispatch({type: actionTypes.CREATE});
      // };
    },

    read() {
      return (dispatch, getState) => {

      };
    },

    update() {
      return (dispatch, getState) => {

      };
    },

    delete() {
      return (dispatch, getState) => {

      };
    },
  }
}
